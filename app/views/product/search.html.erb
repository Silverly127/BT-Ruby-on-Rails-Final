<% content_for :content do %>
<div class="container">
	<hr class="border-danger">
	<h4 class="text-danger">TÌM KIẾM CHO TỪ KHÓA: <%= @vls %></h4><br>
	<% if(@searchproducts.count==0) %>
	<div style="height: 450px">
		Không có sản phẩm để hiển thị!
	</div>
	<% else %>
	<div class="row ">
		<% @searchproducts.each do |product| %>
		<%= render "elements/product", :product => product  =%>
		<% end %>
	</div>
	<% end %>
</div>
	<br>
	<%= will_paginate @searchproducts, renderer: WillPaginate::ActionView::BootstrapLinkRenderer, previous_label: "Trước", next_label: "Sau", inner_window: 1, outer_window: 1, list_classes: %w(pagination justify-content-end text-danger) %>
<% end %>